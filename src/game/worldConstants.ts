/** Shared world/canvas sizing, referenced by PhaserGame config and every scene. */
export const WORLD_SIZE = { width: 960, height: 640 };

/** How close the player must be to a signpost/NPC/interactable to act on it. */
export const INTERACT_RADIUS = 64;
